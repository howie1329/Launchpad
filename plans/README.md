# Implementation Plans

Generated by the improve skill on 2026-06-17. Execute in the order below unless dependencies say otherwise. Each executor should read the assigned plan fully before starting, honor its STOP conditions, and update the status row when done.

## Execution Order & Status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| 001 | Establish a test baseline for critical workspace flows | P1 | M | - | DONE |
| 002 | Make daily AI caps enforceable under concurrent requests | P1 | M | 001 | DONE |
| 003 | Prevent Launchpad Action state drift across Composio and Convex | P1 | S-M | 001 | TODO |
| 004 | Triage and reduce high-risk dependency advisories | P2 | M | 001 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency Notes

- 001 was approved with a verification caveat: `npm test` and `npm run check` pass, touched files pass targeted Prettier and ESLint checks, but repository-wide `npm run lint` still fails on pre-existing out-of-scope formatting issues.
- 002 was approved with the same lint caveat: `npm test`, `npm run check`, `npm run build`, and targeted ESLint/Prettier for Plan 002 files pass, but repository-wide `npm run lint` still fails on pre-existing out-of-scope formatting issues.
- 002 depends on 001 because budget enforcement touches AI request flow and Convex usage accounting; tests should exist before changing that behavior.
- 003 depends on 001 because the fix relies on route/helper tests around Launchpad Action request and failure handling.
- 004 depends on 001 so dependency updates have at least a small behavioral regression suite beyond typecheck/lint/build.

## Findings Considered And Rejected

- Bounded request body handling on AI/webhook endpoints: valid hardening item, but lower leverage than the selected P1 plans. Revisit after tests and budget/action fixes land.
- Braintrust tracing production guardrail: valid privacy hardening item, but current code already requires explicit env enablement and docs warn about trace contents. Revisit if tracing is enabled outside local/engineering environments.

## Source Audit Notes

- Planned against commit `df53974`.
- Verification commands discovered during recon: `npm run check`, `npm run lint`, `npm run build`; Plan 001 adds `npm test`.
- No source code was modified when these plans were created.
